<Window x:Class="OpticEMS.MVVM.View.Windows.UpdateDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:settings="clr-namespace:OpticEMS.MVVM.View.Settings"
        xmlns:local="clr-namespace:OpticEMS"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        Title="System Update" 
        Height="300" Width="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Foreground="White"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Storyboard x:Key="WindowOpeningAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0.0" To="1.0" Duration="0:0:0.3"/>

            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                         From="0.8" To="1.0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                         From="0.8" To="1.0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowOpeningAnimation}"/>
        </EventTrigger>
    </Window.Triggers>

    <materialDesign:Card Padding="16" Background="{StaticResource MaterialDesignLightSeparatorBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       Text="Update"/>
                <TextBlock Style="{StaticResource MaterialDesignCaptionTextBlock}" 
                       Text="Select or drag the update ZIP archive" Margin="0,4,0,0"/>
            </StackPanel>

            <Border Grid.Row="1"
                Background="{DynamicResource MaterialDesignDarkSeparatorBackground}"
                BorderBrush="{DynamicResource MaterialDesignDarkSeparatorBackground}"
                BorderThickness="2"
                CornerRadius="8"
                AllowDrop="True"
                Drop="Border_Drop"
                DragOver="Border_DragOver"
                DragLeave="Border_DragLeave"
                Cursor="Hand"
                MouseDown="Border_MouseDown">

                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDarkSeparatorBackground}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="ZipBox" 
                                             Width="48" Height="48" 
                                             HorizontalAlignment="Center" 
                                             Foreground="White"/>

                    <TextBlock Text="{Binding SelectedFileName, FallbackValue='Drag and drop archive here or click to select'}" 
                           Margin="0,12,0,0"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           FontWeight="Medium"
                           TextWrapping="Wrap"/>
                </StackPanel>

            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Content="Cancel"
                        Command="{Binding CancelCommand}"
                        Margin="0,0,8,0">
                    
                    <Button.CommandParameter>
                        <System:Boolean xmlns:System="clr-namespace:System;assembly=mscorlib">False</System:Boolean>
                    </Button.CommandParameter>
                </Button>

                <Button Style="{StaticResource MaterialDesignFlatMidBgButton}"
                        Content="Update"
                        Command="{Binding StartUpdateCommand}"
                        IsEnabled="{Binding CanUpdate}"
                        Width="120"/>
            </StackPanel>

        </Grid>
    </materialDesign:Card>
</Window>
